#include<bits/stdc++.h>
using namespace std;

int main()
{
    int n;
    cin >> n;
    int k;
    cin >> k;
    //we just have to divide n with k and the quotient will be the answere
    cout<<n/k;
}